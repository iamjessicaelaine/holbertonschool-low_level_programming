#include "main.h"

/**
 * mul -  funtion multiplies two integers
 * @a: a is an integer
 * @b: b is an integer
 * Return: return j for success
*/

int mul(int a, int b)
{
int j; 
j = a * b;
return (j);
}
