#include "lists.h"

/**
 * insert_nodeint_at_index - inserts a new node at a given position
 * @head: pointer to the pointer of linked list
 * @idx: the index of the list where the new node should be added, stars at 0
 * @newn: integer to become new node
 * Return: the address of the new node or NULL if it fails
 */

listint_t *insert_nodeint_at_index(listint_t **head, unsigned int idx, int newn)
{

}
