#include "lists.h"

/**
 * delete_nodeint_at_index - deletes the node at index index of listint_t list
 * @head: points to pointer of linked list
 * @index: index of the node that should be deleted and starts at index
 * Return: 1 if succssful, -1 if it fails
 */

int delete_nodeint_at_index(listint_t **head, unsigned int index)
{

}
